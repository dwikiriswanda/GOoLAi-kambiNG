package main
import (
"fmt"
)
func main (){
	
	var p,i int
	var min,nilai,sum,rataPeserta float32
	
	fmt.Scanln(&min)
	
	fmt.Scan(&p)
	i = 0
	for i < 3*p{
		fmt.Scan(&nilai)
		sum = sum + nilai
		i = i + 1
		
		for(s)
		
	}
	rataPeserta = sum / float32(3*p)
	
	fmt.Printf("Rata-rata Ahmad adalah %.3f. Menang? %v\n",rataAhmad, rataAhmad > rataBadrun )
	fmt.Printf("Rata-rata Badrun adalah %.3f. Menang? %v\n",rataBadrun, rataBadrun > rataAhmad )
	
}